#include <stdio.h>
#include <Windows.h>

//文本代码 -》可执行程序（二进制文件）->双击启动程序
//生成可执行文件程序

int main(void) {
	printf("hello wolrd\n");
	system("pause");
	//system("cle");  //清楚屏幕
	//Sleep(1000)
}



main.c			
main_02.c   变量常量 作用域生命周期  枚举常量
main_03.c   操作符   （源码反码补码） ~ （类型） << >>  sizeof
main_04.c   指针，结构体   goto
//在输入环境中，一些其他的变量；使用getchar来处li
main_05.c   循环  阶乘 ，二分查找函数
main_06.c   函数 ，二分查找函数,闰年；素数
main_07.c   函数的嵌套；链式访问
main_08.c   递归（feibo ，阶乘，汉诺塔问题，青蛙跳台阶）  函数（递归练习）
main_09.c   一维数组,二位数组
main_10.c   冒泡；数组名的三种情况
main_11.c   操作符（
			算数操作符 + - * / %  
			位移操作符  >>  <<  不改变源数据
			按位操作符  &（按位与） |（按位或） ^（按位异或） 2进制位  操作数位整数 
			赋值操作符 = += -= *= /= <<=  >>= %= &= ^= |=
			单目操作符 ！ - + &(取地址) *（解地址） sizeof ~（按位取反） ++ -- （类型）
			& 取地址  *拿值
			(类型)  强转换
			结构体   允许我们自己定义一个类型
			隐式转换；   为什么要 整形提升
							   整形提升
							   整形提升
			算术转换   转换遵循向上转换  比长度；相同长度比精度
			操作符的属性：
				/**
				* 1.操作符优先级  (仅仅取0决于部分)   
				*	(表达式)  函数掉用（）  数组下标引用[]  访问结构成员.  访问结果指针的成员 ->
				*    val++   val--  ！（逻辑反）  ~（按位取反）  + -   ++val  --val
				*    *（解地址）   &（取地址）    sizeof()  （类型）   
				*    *（乘）   /  %  +   -   <<  >>   >  >=  >  <= == !=  

				***    &(位与 二进制1 1为1)  ^（位异或 相同为0 相异为1）  |（位或 有1为1）
				
				*	 && || ?:(条件、三目操作符)  =  +=  -=  *=  /=  %= <<=  >>=  &=  ^=  |= 
				*    , 
				* 
				* 2.操作符的结合性
				* 3.是否控制求值顺序
				* 
				*  两个相邻的操作符先执行哪个？
				*		取决于他们的优先级；
				*（优先级相同）取决于他们的结合性
				**/
			汇编操作  运行-鼠标右击-汇编代码      eax 寄存器    add存入
			）
			数组作为参数传入函数中； 传入的是数组第一个元素的地址（指针）  32位 4B  64位 8B
main_12.c   指针，野指针，指针运算；二级指针，指针数组
main_13.c   结构体
main_14.c   调试  （*越界-死循环，*模拟字符串相关函数） const修饰指针的三种形式
main_15.c   算数作业
				((n>>1) & 1) == 1
				n=n&(n-1)  拿二进制里面的1
				n&(n-1)==0 是不是2的n次方

				int字节  指针字节  内存最大存储空间
			32位  4        4        2^32
			64位  4        8        2^64


main_16.c ：
int short char  limits.h定义整型家族的取值范围
float double    float.h 定义浮点型家族的取值范围 


main_16.c   数据的存储 （大端小端字节序 ；整型变量提升；）  char  int 的有符号数与无符号数 整形十进制八进制 打印
			//大小端字节序指的是数据在电脑上存储的字节为单位的顺序
			//高字节序（在前：低）   大端
			//低字节序（在前：低）   小端

main_17.c   数据的存储 浮点数
main_18.c   字符指针 数组指针 指针数组 以及传参形式
main_19.c   初阶指针作业讲解（喝汽水，水仙花，打印菱形），调试作业讲解（越界死循环） 
			数据存储作业  char与int数据类型的范围限制（255）
			杨辉三角	
			发散思维题	
main_20.c   一级、二级指针参数传递